## 2026-08-10 - Using aria-disabled for disabled buttons with tooltips
 **Learning:** Native `disabled` attributes prevent hover events, meaning users couldn't see a tooltip (`title` attribute) explaining why an option was disabled. Screen readers also wouldn't read the `title` attribute on disabled elements properly in all cases, or standard browser features wouldn't work.
 **Action:** Instead of `disabled`, use `aria-disabled={true}` and manually prevent action in the `onClick` handler by calling `e.preventDefault()` and returning early. Also, use Tailwind's `aria-disabled:` variant to style it.## 2026-08-14 - Applying accessible selection state and tooltips to VideoSquisher
**Learning:** It was confirmed that using aria-disabled and aria-pressed was correct on disabled button groups, specifically preventing clicks manually instead of relying on the native attribute, because the standard disabled tag removes them from the document flow and prevents mouse hover. Similarly, adding aria-pressed explicitly makes screen readers aware of which option in custom button groups is currently active.
**Action:** We should apply this pattern consistently for all custom group selections.
