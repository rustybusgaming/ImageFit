## 2024-05-24 - Tooltips for disabled buttons
**Learning:** Disabling UI controls without explanation causes friction. Users may think the app is broken.
**Action:** Adding `title` tooltips to `disabled` elements gives immediate, native feedback without cluttering the UI.
